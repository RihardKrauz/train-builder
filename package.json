{
  "name": "train-builder",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js",
    "deploy": "git add -A && git commit -m \"autocommit\" && git push heroku master",
    "logs": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^7.11.0"
  }
}
